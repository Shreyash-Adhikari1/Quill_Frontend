# Quill Frontend

Quill is a minimal note-sharing interface built with Next.js, TypeScript, and Tailwind CSS.

Local development runs over HTTPS because the backend uses httpOnly Secure cookies and CSRF cookies. Keeping the frontend on HTTPS makes auth, cookie delivery, and CSRF behavior match production-like security conditions.
